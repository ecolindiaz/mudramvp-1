# Run 010 [comparison]
> **Query:** PostgreSQL vs CockroachDB vs TiDB for globally distributed SQL
> **Tokens:** 46,958 (reasoning: 3,797)

## Searches (9)

### Step 1: `CockroachDB serializable isolation level by default Raft consensus multi-region tables REGIONAL BY ROW follower reads`
  - CockroachDB serializable isolation level by default Raft consensus multi-region tables REGIONAL BY ROW follower reads
  - TiDB transaction isolation level snapshot isolation pessimistic transactions placement rules multi-region
  - TiDB HTAP TiFlash columnar storage real-time analytics documentation
  - PostgreSQL replication single primary streaming replication logical replication read replicas multi-region limitations

### Step 2: `TiDB placement rules data placement multi-DC multi-region followers read stale read documentation`
  - TiDB placement rules data placement multi-DC multi-region followers read stale read documentation
  - TiDB stale read AS OF TIMESTAMP or tidb_read_staleness documentation
  - CockroachDB license BSL Community License 2025 2024
  - Citus extension distributed PostgreSQL sharding reference tables coordinator worker architecture

### Step 3: `TiDB placement rules documentation 'placement rules' TiKV TiDB multi-region`
  - TiDB placement rules documentation 'placement rules' TiKV TiDB multi-region
  - TiDB geo-distributed deployment placement rules leader follower learner replicas documentation
  - TiDB docs placement rules SQL 'ALTER PLACEMENT POLICY'
  - TiDB Cloud multi-region deployment documentation

### Step 4: `TiDB TiFlash columnar storage HTAP documentation real-time analytics`
  - TiDB TiFlash columnar storage HTAP documentation real-time analytics
  - TiDB HTAP TiFlash 'Row-based TiKV' 'Columnar TiFlash'
  - CockroachDB HTAP columnstore analytics capabilities compared to TiFlash
  - CockroachDB architecture raft ranges MVCC distributed transactions

### Step 5: `TiDB docs TiFlash overview learner replica MPP columnar storage`
  - TiDB docs TiFlash overview learner replica MPP columnar storage
  - docs.pingcap.com TiFlash 'ALTER TABLE' 'set tiflash replica'
  - TiFlash MPP engine TiDB docs
  - TiDB HTAP overview TiKV TiFlash docs

### Step 6: `PostgreSQL documentation streaming replication primary standby read-only hot standby`
  - PostgreSQL documentation streaming replication primary standby read-only hot standby
  - PostgreSQL documentation logical replication publication subscription does not provide conflict resolution multi-master
  - PostgreSQL license permissive PostgreSQL License documentation
  - CockroachDB PostgreSQL wire protocol compatibility statement documentation

### Step 7: `PostgreSQL logical replication does not support conflict detection resolution multi-master`
  - PostgreSQL logical replication does not support conflict detection resolution multi-master
  - PostgreSQL logical replication limitations DDL sequences conflict resolution documentation
  - PostgreSQL logical replication bidirectional not supported built-in
  - PostgreSQL documentation logical replication restrictions 'not a multimaster'

### Step 8: `pingcap tidb license Apache 2.0`
  - pingcap tidb license Apache 2.0
  - TiDB Docs license Apache 2.0
  - PostgreSQL License BSD-like permissive
  - TiDB MySQL compatible documentation

### Step 9: `PostgreSQL documentation current hot standby read-only eventually consistent with primary`
  - PostgreSQL documentation current hot standby read-only eventually consistent with primary
  - PostgreSQL documentation streaming replication read-only standby hot standby parameter
  - PostgreSQL documentation synchronous replication across regions latency
  - PostgreSQL physical replication read-only standby cannot accept writes
