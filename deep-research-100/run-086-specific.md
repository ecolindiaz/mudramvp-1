# Run 086 [specific]
> **Query:** Setting up Temporal.io workflow engine with custom codec for encrypted payloads and multi-cluster replication
> **Tokens:** 99,602 (reasoning: 10,149)

## Searches (22)

### Step 1: `Temporal payload codec encryption data converter codec server documentation`
  - Temporal payload codec encryption data converter codec server documentation
  - Temporal SDK payload codec example encryption Go Java TypeScript
  - Temporal multi-cluster replication global namespaces configuration remote clusters
  - Temporal server multi cluster replication cluster metadata config yaml

### Step 2: `site:temporal.io payload codec remote codec server temporal web ui`
  - site:temporal.io payload codec remote codec server temporal web ui
  - site:docs.temporal.io payload codec encryption data converter
  - site:docs.temporal.io global namespaces multi-cluster replication remote cluster upsert-remote-cluster
  - site:temporal.io helm charts multi cluster replication remote clusters global namespace configuration

### Step 3: `site:docs.temporal.io codec server decode encode payloads`
  - site:docs.temporal.io codec server decode encode payloads
  - site:docs.temporal.io payload codec data converter typescript python go
  - site:docs.temporal.io global namespace failoverVersionIncrement clusterMetadata clusterInformation
  - site:docs.temporal.io tctl admin upsert-remote-cluster

### Step 4: `Temporal "codec server" documentation`
  - Temporal "codec server" documentation
  - Temporal "PayloadCodec" documentation
  - Temporal "global namespace" replication documentation
  - temporal operator cluster upsert-remote-cluster documentation

### Step 5: `data encryption codec server site:docs.temporal.io production deployment`
  - data encryption codec server site:docs.temporal.io production deployment
  - site:docs.temporal.io "Codec Server" "Data Encryption"
  - site:temporal.io "data-encryption" "codec" "Payload Codec"
  - site:github.com/temporalio/documentation data-encryption.mdx codec server

### Step 6: ``

### Step 7: ``

### Step 8: ``

### Step 9: `enableGlobalNamespace clusterMetadata failoverVersionIncrement clusterInformation site:raw.githubusercontent.com/temporalio/temporal`
  - enableGlobalNamespace clusterMetadata failoverVersionIncrement clusterInformation site:raw.githubusercontent.com/temporalio/temporal
  - site:raw.githubusercontent.com/temporalio/temporal currentClusterName masterClusterName replicationConsumer type rpcAddress
  - site:raw.githubusercontent.com/temporalio/helm-charts clusterMetadata enableGlobalNamespace clusterInformation
  - global namespace temporal operator cluster upsert remote cluster documentation

### Step 10: ``

### Step 11: `site:raw.githubusercontent.com/temporalio/documentation main multi-cluster replication global namespace`
  - site:raw.githubusercontent.com/temporalio/documentation main multi-cluster replication global namespace
  - site:raw.githubusercontent.com/temporalio/documentation main "Global Namespaces"
  - site:raw.githubusercontent.com/temporalio/documentation main "enableGlobalNamespace"
  - site:docs.temporal.io "Global Namespace" failover

### Step 12: `Temporal documentation global namespace failover version increment`
  - Temporal documentation global namespace failover version increment
  - site:docs.temporal.io global namespace multi-cluster replication
  - site:github.com/temporalio/documentation "Global Namespace" "failover"
  - site:raw.githubusercontent.com/temporalio/documentation main "multi-cluster"

### Step 13: `temporal operator cluster upsert command flags name frontend-address enable-connection`
  - temporal operator cluster upsert command flags name frontend-address enable-connection
  - site:docs.temporal.io "operator cluster upsert"
  - site:github.com/temporalio/cli operator cluster upsert
  - site:raw.githubusercontent.com/temporalio/cli main "cluster upsert"

### Step 14: `site:docs.temporal.io operator cluster upsert-remote-cluster`
  - site:docs.temporal.io operator cluster upsert-remote-cluster
  - site:docs.temporal.io "upsert" "remote cluster" Temporal CLI
  - site:docs.temporal.io "global namespaces" "clusterMetadata"
  - site:docs.temporal.io "multi-cluster" "enableGlobalNamespace"

### Step 15: `enableGlobalNamespace site:github.com/temporalio/documentation`
  - enableGlobalNamespace site:github.com/temporalio/documentation
  - "clusterMetadata" "enableGlobalNamespace" temporalio/documentation
  - "Global Namespace" site:github.com/temporalio/documentation docs
  - "multi-cluster" site:github.com/temporalio/documentation docs

### Step 16: `AddOrUpdateRemoteClusterRequest temporal api proto`
  - AddOrUpdateRemoteClusterRequest temporal api proto
  - site:raw.githubusercontent.com/temporalio/api master AddOrUpdateRemoteCluster
  - site:raw.githubusercontent.com/temporalio/api main AddOrUpdateRemoteCluster
  - site:raw.githubusercontent.com/temporalio/temporal master AddOrUpdateRemoteCluster

### Step 17: `repo:temporalio/cli "operator cluster upsert"`
  - repo:temporalio/cli "operator cluster upsert"
  - site:github.com/temporalio/cli operator cluster upsert frontend-address enable-connection
  - "temporal operator" "cluster" "upsert" "frontend-address"
  - go.temporal.io/cli operator cluster upsert

### Step 18: ``

### Step 19: ``

### Step 20: `site:raw.githubusercontent.com/temporalio/helm-charts main clusterMetadata enableGlobalNamespace replicationConsumer`
  - site:raw.githubusercontent.com/temporalio/helm-charts main clusterMetadata enableGlobalNamespace replicationConsumer
  - site:github.com/temporalio/helm-charts clusterMetadata replicationConsumer
  - temporal helm chart clusterMetadata replicationConsumer type rpc
  - site:raw.githubusercontent.com/temporalio/helm-charts main values.yaml clusterMetadata

### Step 21: `masterClusterName Temporal global namespace master cluster documentation`
  - masterClusterName Temporal global namespace master cluster documentation
  - "masterClusterName" site:raw.githubusercontent.com/temporalio/temporal
  - "masterClusterName" site:raw.githubusercontent.com/temporalio/documentation
  - masterClusterName failoverVersionIncrement clusterInformation Temporal

### Step 22: `go.temporal.io/sdk converter NewCodecDataConverter PayloadCodec`
  - go.temporal.io/sdk converter NewCodecDataConverter PayloadCodec
  - site:pkg.go.dev go.temporal.io/sdk/converter NewCodecDataConverter
  - site:pkg.go.dev go.temporal.io/sdk/converter PayloadCodec
  - temporal go sdk CodecDataConverter NewCodecDataConverter
